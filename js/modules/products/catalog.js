// PRODUCT CATALOG MODULE
// Owner: Catalog Dev

// TODO:
// - fetch products.json
// - render product cards
// - search + filter

console.log("catalog module loaded");

//Example of fetching products data and rendering (to be implemented in render function) :
// fetch("/data/products.json")
//   .then(r => r.json())
//   .then(products => {
//     render(products);
//   });
